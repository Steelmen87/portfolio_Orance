import React from 'react';
import s from "../AppStyle.module.css";

const Blog = () => {
    return (
        <div className={s.container}>
            Blog
        </div>
    );
};

export default Blog;